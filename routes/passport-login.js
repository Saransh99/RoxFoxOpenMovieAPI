const express = require('express');
const router = express.Router();
const passport = require('passport');

// this will be the routes for the login using the passport one 

router.get('/login', (req, res) => {

});

module.exports = router;